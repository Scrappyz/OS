## os::path::normalizePath
Defined in header `os.hpp`

| Declarations |
| --- |
| std::string normalizePath(const std::filesystem::path& path) |

## Parameters
`path` - the path to normalize

## Return Value
Returns the converted to string to work with the current operating system.